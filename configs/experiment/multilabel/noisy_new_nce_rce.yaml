# @package _global_

defaults:
  - /model: base
  - override /data: holdout
  - override /callbacks: with_early_stopping
  - _self_

research_name: SurfaceDefectDetection
experiment_name: noisy_new
run_name_postfix: nce_rce

n_runs: 10

model:
  num_classes: 10
  net:
    _target_: src.models.components.mobile_net.MobileNet
    pretrained: true
    variant: small
  optimizer:
    _target_: torch.optim.SGD
    _partial_: true
    lr: 0.00492
    momentum: 0.797
    weight_decay: 8.862e-05
  scheduler:
    _target_: torch.optim.lr_scheduler.CosineAnnealingLR
    T_max: ${trainer.max_epochs}
  criterion:
    _target_: src.models.components.loss.NCEandRCE
    alpha: 0.167
    beta: 0.244

data:
  batch_size: 32
  weight_alpha: 0.958
  transforms: medium
  image1k_norm: true
  train_path: /srv/defectDetectionDataset/surfaceClassification/noisy_clustered_new/train
  val_path: /srv/defectDetectionDataset/surfaceClassification/noisy_clustered_new/val
  test_path: /srv/defectDetectionDataset/surfaceClassification/noisy_clustered_new/test

callbacks:
  early_stopping:
    patience: 30