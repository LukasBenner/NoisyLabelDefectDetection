# @package _global_

defaults:
  - /model: base
  - override /data: holdout
  - override /callbacks: with_early_stopping
  - _self_

experiment_name: "Baseline_Max"
run_name_postfix: "cluster_split"

n_runs: 5

model:
  num_classes: 12
  net:
    _target_: src.models.components.mobile_net.MobileNet
    pretrained: true
    variant: small
  optimizer:
    _target_: torch.optim.AdamW
    _partial_: true
    lr: 0.00032
    weight_decay: 1e-5
  scheduler: Null
  criterion:
    _target_: src.models.components.loss.CELoss

data:
  batch_size: 32
  image1k_norm: true
  transforms: baseline
  train_path: /srv/defectDetectionDataset/multiclassClassification/baseline/train
  val_path: /srv/defectDetectionDataset/multiclassClassification/baseline/val
  test_path: /srv/defectDetectionDataset/multiclassClassification/baseline/test

callbacks:
  early_stopping:
    patience: 50