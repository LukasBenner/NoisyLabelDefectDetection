# @package _global_

run_name: "nce_rce_${now:%Y-%m-%d_%H-%M-%S}"


optimizer: 
  lr: 0.00032
  weight_decay: 1.5e-5
scheduler:
  _target_: torch.optim.lr_scheduler.CosineAnnealingLR
  T_max: ${num_epochs}
loss:
  _target_: src.models.components.loss.NCEandRCE
  alpha: 10
  beta: 0.1