_target_: src.data.no_test_datamodule.NoTestDataModule

defaults:
  - default

data_path: surface
val_split: 0.2
batch_size: 32
num_workers: 4
pin_memory: True
use_weighted_sampling: False
seed: ${seed}
